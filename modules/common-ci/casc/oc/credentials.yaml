credentials:
  # 1.3.1 | Authentication and Authorization. Crendentials
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              description: "GH-token-merck"
              id: "GH-token-merck"
              password: ${readFile:${SECRETS}/githubToken}
              scope: GLOBAL
              username: ${readFile:${SECRETS}/githubUser}
              usernameSecret: true
